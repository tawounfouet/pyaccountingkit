# PyAccountingKit examples

> **Bootstrap status (`0.0.1`)**: the directories below are roadmap placeholders, not runnable examples yet. Runnable examples will be activated only when their backing public APIs and tests are implemented.

| Directory | Status | `0.0.1` behavior |
|---|---|---|
| `basic_accounting/` | Future scaffold | Non-runnable in `0.0.1` |
| `consolidation/` | Future scaffold | Non-runnable in `0.0.1` |
| `fec_import/` | Future scaffold | Non-runnable in `0.0.1` |
| `financial_reporting/` | Future scaffold | Non-runnable in `0.0.1` |
| `reconciliation/` | Future scaffold | Non-runnable in `0.0.1` |
| `subledger/` | Future scaffold | Non-runnable in `0.0.1` |

This explicit classification prevents the repository scaffold from being mistaken for implemented functionality.
