# Consolidation

> **Status: future example scaffold.**

PyAccountingKit `0.0.1` is the Repository Bootstrap milestone and does not yet expose the accounting-domain APIs required by this example. This directory is intentionally non-runnable until the milestone owning the corresponding feature implements and tests it.

Do not use this directory as evidence that the feature is implemented.
